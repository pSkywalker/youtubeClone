import { DataFormatterVideoPostedPipe } from './data-formatter-video-posted.pipe';

describe('DataFormatterVideoPostedPipe', () => {
  it('create an instance', () => {
    const pipe = new DataFormatterVideoPostedPipe();
    expect(pipe).toBeTruthy();
  });
});
